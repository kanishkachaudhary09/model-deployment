# model-deployment
This project demonstrates the deployment of a trained Machine Learning model as a REST API using FastAPI and containerization with Docker. The objective is to make the model accessible through HTTP endpoints, allowing users or applications to send input data and receive predictions in real time. Docker is used to package the application 
